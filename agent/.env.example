# Credentials for pi extensions. See docs/adr/0042.
#
# Copy this file to agent/.env and replace the placeholders with real keys:
#     cp agent/.env.example agent/.env
# The real agent/.env is gitignored and never committed.

# Provider key used by agent/extensions/litellm.ts (Authorization: Bearer <key>).
LITELLM_API_KEY=sk-your-litellm-provider-key

# Gateway key used by agent/mcp.json (sent as the x-litellm-api-key header).
# A DIFFERENT credential from LITELLM_API_KEY -- see docs/adr/0042.
LITELLM_MCP_KEY=your-litellm-gateway-key

# Gateway base URL used by agent/mcp.json (the ${LITELLM_MCP_URL} placeholder).
LITELLM_MCP_URL=https://your-litellm-gateway/mcp
